export interface Store {
  id: number;
  location: string;
  period: number;
  title: string;
  body: string;
  opening: string;
  images: string[];
  link: string;
  latitude: number;
  longitude: number;
}
